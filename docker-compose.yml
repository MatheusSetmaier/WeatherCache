services:
  api:
    build: .
    container_name: weathercache_api
    ports:
      - '8000:8000'
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    env_file:
      - .env

  redis:
    image: redis:latest
    container_name: weathercache_redis
    ports:
      - '6379:6379'